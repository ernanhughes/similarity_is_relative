# Option B canonical row-selection v2 execution marker

This temporary marker records that the draft PR has started the two fresh canonical selection runs. It must be removed after the canonical artifacts and completed checkpoint are committed.
